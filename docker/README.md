# Docker

Docker Compose lance les dependances locales de Pointage360:

- PostgreSQL sur `localhost:5432`
- Redis sur `localhost:6379`
- MinIO API sur `localhost:9000`
- MinIO Console sur `localhost:9001`

Commande:

```bash
docker compose up -d
```
