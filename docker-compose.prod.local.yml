services:
  caddy:
    volumes:
      - ./docker/Caddyfile.local:/etc/caddy/Caddyfile:ro

  web:
    build:
      args:
        NEXT_PUBLIC_API_URL: /api
    environment:
      NEXT_PUBLIC_API_URL: /api

  api:
    ports:
      - "4000:4000"